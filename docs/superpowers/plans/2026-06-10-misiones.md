# Rebanada 5 — Sistema de misiones (fase previa al ritual) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir una fase MISSIONS previa al ritual: los 8 bots reparan monitores repartidos (siempre ocupados, conocidos desde el inicio); al completarlas todas se desbloquea el ritual de R2 (gather → channel → ganar). El rugido pasa a acosar (cordura↓ + ralentiza) en vez de dispersar.

**Architecture:** Extender la máquina de estados pura `js/ritual.js` con una fase `MISSIONS` + estado `missions[]` + `workMission` + gate MISSIONS→GATHER + rama de `assignRitualRoles` (rol REPAIR). `js/main.js` coloca estaciones (TV GLB), enruta a los REPAIR, llama `workMission`, renderiza el progreso y cambia `roar()` de `flee` a `shaken`. `js/ai.js`/`js/abilities.js` no cambian.

**Tech Stack:** JS vanilla ESM, Three.js 0.160 por CDN (sin build), Node test runner (`node --test`), verificación manual vía CDP/headless.

**Spec:** `docs/superpowers/specs/2026-06-10-misiones-design.md`.

---

## Estructura de archivos

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `js/ritual.js` | Fase MISSIONS + `missions[]` + `workMission`/`allMissionsDone` + gate + rol REPAIR. | Modificar |
| `test/ritual.test.js` | Migrar firma de `createRitual` + tests de misiones. | Modificar |
| `js/main.js` | Estaciones (TV), candidatos REPAIR, `workMission`, render, `roar`→`shaken`, HUD/minimapa. | Modificar |

---

## Fase A — Núcleo puro `js/ritual.js` (TDD)

### Task 1: Fase MISSIONS, rol REPAIR y nueva firma de `createRitual`

**Files:**
- Modify: `js/ritual.js`
- Test: `test/ritual.test.js`

- [ ] **Step 1: Migrar las llamadas existentes + tests que fallan**

`createRitual` pasa a recibir `missionCells` como PRIMER argumento. Para no romper los tests de R2, en `test/ritual.test.js` reemplaza **todas** las apariciones de `createRitual(` por `createRitual([], ` (todas las llamadas actuales empiezan por `objectCells`, así que anteponer `[]` = sin misiones = arranca en GATHER como antes). El `import` (`import { createRitual, ... }`) no lleva paréntesis y no se ve afectado.

Luego añade al final de `test/ritual.test.js`:

```js
test('createRitual con misiones empieza en MISSIONS; sin misiones, en GATHER', () => {
  const conM = createRitual([[1, 1]], [[2, 2]], [0, 0]);
  assert.equal(conM.phase, PHASE.MISSIONS);
  assert.equal(conM.missions.length, 1);
  assert.equal(conM.needMissions, 1);
  const sinM = createRitual([], [[2, 2]], [0, 0]);
  assert.equal(sinM.phase, PHASE.GATHER);
  assert.equal(sinM.missions.length, 0);
});
```

- [ ] **Step 2: Verificar que falla**

Run: `npm test`
Expected: FAIL — los tests de R2 fallan (firma vieja) y/o `conM.missions` indefinido.

- [ ] **Step 3: Implementación**

En `js/ritual.js`, cambia `PHASE`, `RROLE` y `createRitual`:

```js
export const PHASE = { MISSIONS: 'MISSIONS', GATHER: 'GATHER', CHANNEL: 'CHANNEL', DONE: 'DONE' };
// Roles de fase: reutiliza los de R1 + añade los específicos de la ruta de victoria.
// (CHANNELER != PHASE.CHANNEL: el rol del canalizador usa otra cadena para no colisionar con la fase.)
export const RROLE = { ...ROLES, REPAIR: 'REPAIR', FETCH: 'FETCH', CHANNELER: 'CHANNELER', DEFEND: 'DEFEND', DISTRACT: 'DISTRACT' };

export function createRitual(missionCells, objectCells, altarCell, opts = {}) {
  const p = { ...RCFG, ...opts };
  return {
    altar: { gx: altarCell[0], gz: altarCell[1] },
    missions: missionCells.map(([gx, gz], i) => ({ id: i, gx, gz, progress: 0, done: false })),
    objects: objectCells.map(([gx, gz], i) => ({ id: i, gx, gz, status: OBJ.ON_MAP, carrier: null, homeGx: gx, homeGz: gz })),
    phase: missionCells.length ? PHASE.MISSIONS : PHASE.GATHER,
    channel: 0,
    needMissions: missionCells.length,
    needObjects: objectCells.length,
    needChannelers: p.NEED_CHANNELERS,
    channelTime: p.CHANNEL_TIME,
    penalty: p.CHANNEL_PENALTY,
  };
}
```

- [ ] **Step 4: Verificar que pasa**

Run: `npm test`
Expected: PASS (tests de R2 migrados + el nuevo de fases).

- [ ] **Step 5: Commit**

```bash
git add js/ritual.js test/ritual.test.js
git commit -m "feat(misiones): fase MISSIONS + rol REPAIR + nueva firma createRitual"
```

---

### Task 2: `workMission` + `missionsDoneCount` + `allMissionsDone` + gate

**Files:**
- Modify: `js/ritual.js`
- Test: `test/ritual.test.js`

- [ ] **Step 1: Tests que fallan**

Añade a `test/ritual.test.js`:

```js
import { workMission, missionsDoneCount, allMissionsDone } from '../js/ritual.js';

test('workMission sube progreso, clampa y marca done', () => {
  const r = createRitual([[1, 1], [2, 2]], [[5, 5]], [0, 0]);
  assert.equal(workMission(r, 0, 2, 0.2), false); // 2*0.2=0.4 < 1
  assert.ok(Math.abs(r.missions[0].progress - 0.4) < 1e-9);
  assert.equal(workMission(r, 0, 10, 0.2), true); // pasa de 1 -> clamp + done
  assert.equal(r.missions[0].progress, 1);
  assert.equal(r.missions[0].done, true);
  assert.equal(missionsDoneCount(r), 1);
});

test('al completar TODAS las misiones se pasa a fase GATHER', () => {
  const r = createRitual([[1, 1], [2, 2]], [[5, 5]], [0, 0]);
  workMission(r, 0, 10, 1);
  assert.equal(r.phase, PHASE.MISSIONS); // aún queda la 1
  workMission(r, 1, 10, 1);
  assert.equal(allMissionsDone(r), true);
  assert.equal(r.phase, PHASE.GATHER);
});

test('workMission no hace nada fuera de fase MISSIONS', () => {
  const r = createRitual([], [[1, 1]], [0, 0]); // GATHER
  assert.equal(workMission(r, 0, 10, 1), false);
});
```

- [ ] **Step 2: Verificar que falla**

Run: `npm test`
Expected: FAIL — `workMission is not a function`.

- [ ] **Step 3: Implementación**

Añade a `js/ritual.js` (tras `allDeposited`/`depositCarried`, antes de `channelTick`):

```js
export function missionsDoneCount(ritual) {
  return ritual.missions.filter((m) => m.done).length;
}
export function allMissionsDone(ritual) {
  return ritual.missions.length > 0 && missionsDoneCount(ritual) === ritual.missions.length;
}

// Avanza una misión en fase MISSIONS. Al completar TODAS -> phase = GATHER
// (se desbloquea el ritual). Devuelve si la misión quedó hecha.
export function workMission(ritual, id, dt, rate) {
  if (ritual.phase !== PHASE.MISSIONS) return false;
  const m = ritual.missions.find((x) => x.id === id);
  if (!m || m.done) return false;
  m.progress = Math.min(1, m.progress + dt * rate);
  if (m.progress >= 1) m.done = true;
  if (allMissionsDone(ritual)) ritual.phase = PHASE.GATHER;
  return m.done;
}
```

- [ ] **Step 4: Verificar que pasa**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add js/ritual.js test/ritual.test.js
git commit -m "feat(misiones): workMission + gate MISSIONS->GATHER"
```

---

### Task 3: `assignRitualRoles` en fase MISSIONS (REPAIR)

**Files:**
- Modify: `js/ritual.js`
- Test: `test/ritual.test.js`

- [ ] **Step 1: Tests que fallan**

Añade a `test/ritual.test.js`:

```js
test('assignRitualRoles en MISSIONS: todos los vivos a REPAIR', () => {
  const r = createRitual([[1, 1]], [[2, 2]], [0, 0]); // MISSIONS
  const agents = [
    { id: 0, alive: true, bravery: 0.5, gx: 0, gz: 0 },
    { id: 1, alive: false, bravery: 0.8, gx: 1, gz: 1 },
    { id: 2, alive: true, bravery: 0.2, gx: 2, gz: 2 },
  ];
  const roles = assignRitualRoles(agents, r, 0);
  assert.equal(roles.get(0), RROLE.REPAIR);
  assert.equal(roles.get(2), RROLE.REPAIR);
  assert.equal(roles.has(1), false); // muerto, sin rol
});
```

- [ ] **Step 2: Verificar que falla**

Run: `npm test`
Expected: FAIL — el rol devuelto no es REPAIR (cae en la rama GATHER).

- [ ] **Step 3: Implementación**

En `js/ritual.js`, dentro de `assignRitualRoles`, añade la rama de MISSIONS al principio (tras calcular `alive`, antes de la rama `CHANNEL`):

```js
  if (ritual.phase === PHASE.MISSIONS) {
    for (const a of alive) out.set(a.id, RROLE.REPAIR);
    return out;
  }
```

- [ ] **Step 4: Verificar que pasa**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add js/ritual.js test/ritual.test.js
git commit -m "feat(misiones): assignRitualRoles reparte REPAIR en MISSIONS"
```

---

## Fase B — Integración en `js/main.js`

> Verificación manual con servidor local + CDP/headless. Tras cada tarea: `node --check js/main.js` y `npm test`. Usa **anclas de contenido**, no números de línea.

### Task 4: Estaciones de misión (TV) + nueva llamada a `createRitual` + render de progreso

**Files:**
- Modify: `js/main.js`

- [ ] **Step 1: Constantes**

Junto a `const NUM_RITUAL_OBJECTS = 4;` (y `RITUAL_SPREAD`), añade:

```js
const NUM_MISSIONS = 6;
const REPAIR_RATE = 0.12;   // progreso/seg de una misión (~8s con un bot)
const SHAKEN_DUR = 4;       // s que dura el "aturdimiento" por rugido
const SHAKEN_SLOW = 0.4;    // multiplicador de tarea/movimiento mientras shaken
const MISSION_HEIGHT = 0.95; // alto objetivo del monitor
```

- [ ] **Step 2: Registro de mallas de misión**

Junto a `const ritualMeshes = new Map();` (y `altarMesh`/`altarMat`), añade:

```js
const missionMeshes = new Map(); // missionId -> { mesh, mat }
```

- [ ] **Step 3: Colocar misiones en `makeRitual`**

En `makeRitual`, sustituye el bloque de elección de celdas y la llamada a `createRitual` por la versión con misiones:

```js
  const hub = REACH.list[Math.floor(Math.random() * REACH.list.length)];
  const cells = spreadCells(NUM_MISSIONS + NUM_RITUAL_OBJECTS + 1, (x, z) => (Math.abs(x - hub[0]) + Math.abs(z - hub[1])) > RITUAL_SPREAD);
  const [agx, agz] = cells[0], [awx, awz] = worldOf(agx, agz);
  const missionCells = cells.slice(1, 1 + NUM_MISSIONS);
  const objCells = cells.slice(1 + NUM_MISSIONS, 1 + NUM_MISSIONS + NUM_RITUAL_OBJECTS);
  ritual = RIT.createRitual(missionCells, objCells, [agx, agz], { NEED_CHANNELERS: 2 });
```

Y al final de `makeRitual` (tras colocar los objetos rituales), añade la colocación de los monitores de misión:

```js
  // Estaciones de misión = monitores (reusa el TV GLB; respaldo caja).
  for (const m of ritual.missions) {
    const [wx, wz] = worldOf(m.gx, m.gz);
    let mesh, mat;
    if (assets && assets.tv) {
      mesh = placeProp(assets.tv, wx, wz, MISSION_HEIGHT, true);
      mesh.traverse((o) => { if (o.isMesh && !mat) mat = (Array.isArray(o.material) ? o.material[0] : o.material).clone(); });
      mesh.traverse((o) => { if (o.isMesh) o.material = mat; });
    } else {
      mat = new THREE.MeshStandardMaterial({ color: 0x222633, emissive: 0xff3333, emissiveIntensity: 0.6 });
      mesh = new THREE.Mesh(new THREE.BoxGeometry(0.7, MISSION_HEIGHT, 0.5), mat); mesh.position.set(wx, MISSION_HEIGHT / 2, wz);
    }
    if (mat) { mat.emissive = new THREE.Color(0xff3333); mat.emissiveIntensity = 0.6; }
    scene.add(mesh); missionMeshes.set(m.id, { mesh, mat });
  }
```

- [ ] **Step 4: Render de progreso (emissive averiado→reparado)**

En `syncRitualMeshes()`, antes del cierre `}`, añade:

```js
  // Monitores: rojo (averiado) -> verde (reparado) según progreso.
  for (const m of ritual.missions) {
    const mm = missionMeshes.get(m.id); if (!mm || !mm.mat) continue;
    mm.mat.emissive.setRGB(1 - m.progress, 0.2 + 0.7 * m.progress, 0.2);
    mm.mat.emissiveIntensity = 0.5 + 0.4 * m.progress;
  }
```

- [ ] **Step 5: Verificación**

Run: `node --check js/main.js && npm test`
Expected: parsea OK; tests verdes. (Aún no se reparan; eso es Task 5.)

- [ ] **Step 6: Commit**

```bash
git add js/main.js
git commit -m "feat(misiones): estaciones (TV) + createRitual con misiones + render de progreso"
```

---

### Task 5: IA — candidatos REPAIR + trabajar la misión + ralentización shaken

**Files:**
- Modify: `js/main.js`

- [ ] **Step 1: Candidatos para REPAIR**

En `buildCandidates(h)`, dentro del `switch (h.role)`, añade el caso REPAIR (antes de `default`):

```js
    case RIT.RROLE.REPAIR: {
      const inc = ritual.missions.filter((m) => !m.done).map((m) => ({ gx: m.gx, gz: m.gz, bias: 4 }));
      cands = inc.length ? inc : [{ gx: ritual.altar.gx, gz: ritual.altar.gz, bias: 1 }];
      break;
    }
```

- [ ] **Step 2: Incluir shaken en `speedMul`**

En `updateHunter`, localiza `const speedMul = trapped ? 0.5 : 1;` y sustitúyelo por:

```js
  const speedMul = (trapped ? 0.5 : 1) * (h.shaken > 0 ? SHAKEN_SLOW : 1);
```

Justo después, añade el decaimiento del shaken:

```js
  if (h.shaken > 0) h.shaken = Math.max(0, h.shaken - dt);
```

- [ ] **Step 3: Trabajar la misión en la rama IA**

En `updateHunter`, en la rama `else {` (la de la IA, donde hoy se decide goal + pickup/deposit), justo después de fijar `h.goal = newGoal;` y `h.working = -1;`, envuelve la lógica de objetos para que en fase MISSIONS se repare:

```js
    if (ritual.phase === RIT.PHASE.MISSIONS) {
      // REPAIR: si está sobre una misión incompleta, la trabaja; si no, camina hacia la meta.
      let m = null;
      for (const mi of ritual.missions) {
        if (mi.done) continue;
        const [mwx, mwz] = worldOf(mi.gx, mi.gz);
        if (Math.hypot(h.pos.x - mwx, h.pos.z - mwz) <= 1.0) { m = mi; break; }
      }
      if (m) { h.working = 0; RIT.workMission(ritual, m.id, dt, REPAIR_RATE * (h.shaken > 0 ? SHAKEN_SLOW : 1)); }
      else if (h.goal) stepToward(h, h.goal, HUNTER_SPEED * speedMul, dt);
    } else {
      // GATHER: recoger/depositar cruces (R2) — bloque existente sin cambios.
      const [ax, az] = worldOf(ritual.altar.gx, ritual.altar.gz);
      const carried = RIT.objectCarriedBy(ritual, h.id);
      if (carried && !hunting) {
        if (Math.hypot(h.pos.x - ax, h.pos.z - az) <= RIT.RCFG.ALTAR_RANGE) RIT.depositCarried(ritual, h.id);
      } else if (h.role === RIT.RROLE.FETCH) {
        for (const o of ritual.objects) {
          if (o.status !== RIT.OBJ.ON_MAP) continue;
          const [owx, owz] = worldOf(o.gx, o.gz);
          if (Math.hypot(h.pos.x - owx, h.pos.z - owz) <= 0.7) { RIT.pickup(ritual, o.id, h.id); break; }
        }
      }
      if (h.goal) stepToward(h, h.goal, HUNTER_SPEED * speedMul, dt);
    }
```

(Es decir: el bloque actual de pickup/deposit + `stepToward` pasa a ser la rama `else` de `if (ritual.phase === MISSIONS)`. El `pushRecent(h);` que sigue queda igual.)

- [ ] **Step 4: Verificación manual (CDP)**

Run: `npm run dev` → JUGAR.
Expected: los bots van directos a los **monitores** (sin deambular) y los **reparan** (emissive rojo→verde, animación Interact); al completar **todas** pasan a recoger cruces (fase GATHER). Sin errores.

- [ ] **Step 5: Commit**

```bash
git add js/main.js
git commit -m "feat(misiones): IA repara monitores (REPAIR) + ralentizacion shaken"
```

---

### Task 6: Rugido → `shaken` (cordura↓ + ralentiza, sin huir)

**Files:**
- Modify: `js/main.js`

- [ ] **Step 1: Campo `shaken` por agente**

En `makeHunters`, en el objeto `hunters.push({ ... })`, añade `shaken: 0` (junto a `panic: false`).

- [ ] **Step 2: Rediseñar el efecto del rugido**

En `roar()`, sustituye el bloque que aplica `flee`/`dropCarried` a los cercanos:

```js
  for (const h of hunters) {
    if (!h.alive) continue;
    if (Math.hypot(h.pos.x - pos.x, h.pos.z - pos.z) <= SCARE_RANGE) {
      h.shaken = SHAKEN_DUR;                       // no huyen: se desestabilizan
      h.stress = Math.min(1, h.stress + 0.25);
      h.sanity = Math.max(0, h.sanity - 0.15);
    }
  }
```

- [ ] **Step 3: Verificación manual (CDP)**

Run: `npm run dev` → JUGAR → acércate a un bot que repara y ruge (`1`/clic).
Expected: el bot **no huye**; se queda reparando pero **más lento** y con la cordura bajando (más estrés). Sin errores.

- [ ] **Step 4: Commit**

```bash
git add js/main.js
git commit -m "feat(misiones): rugido aturde (cordura + ralentiza) en vez de dispersar"
```

---

### Task 7: HUD + minimapa de misiones

**Files:**
- Modify: `js/main.js`

- [ ] **Step 1: HUD del contador de misiones**

En `updateHUD`, sustituye el bloque que fija `el('missions')` por uno con la fase MISSIONS:

```js
  if (ritual.phase === RIT.PHASE.MISSIONS) {
    el('missions').textContent = RIT.missionsDoneCount(ritual) + '/' + ritual.missions.length;
  } else if (ritual.phase === RIT.PHASE.CHANNEL || ritual.phase === RIT.PHASE.DONE) {
    el('missions').textContent = 'RITUAL ' + Math.round(ritual.channel * 100) + '%';
  } else {
    el('missions').textContent = RIT.depositedCount(ritual) + '/' + ritual.objects.length;
  }
```

- [ ] **Step 2: Minimapa de misiones**

En `drawMinimap()`, tras la marca del altar (`// altar`), añade las marcas de misión:

```js
  for (const m of ritual.missions) {
    mmCtx.fillStyle = m.done ? '#37d67a' : '#ff5555';
    mmCtx.fillRect(m.gx * cs - 1, m.gz * cs - 1, cs + 1.5, cs + 1.5);
  }
```

- [ ] **Step 3: Verificación manual**

Run: `npm run dev` → JUGAR.
Expected: el HUD muestra **misiones X/6** y, al completarlas, pasa a `RITUAL %`; el minimapa marca los monitores (rojo→verde). Sin errores.

- [ ] **Step 4: Commit**

```bash
git add js/main.js
git commit -m "feat(misiones): HUD + minimapa de misiones"
```

---

### Task 8: Verificación integral + README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: `node --test`**

Run: `npm test`
Expected: PASS (ritual + abilities + ai + logic).

- [ ] **Step 2: Verificación integral (CDP/headless)**

Run: `npm run dev` y revisa una partida:
- Los 8 van directos a **reparar monitores** (sin deambular); el emissive sube rojo→verde.
- Al completar **todas** → fase GATHER (recogen cruces → altar → canalizan → ganan).
- **Rugir** cerca ralentiza/baja cordura **sin** hacerles huir.
- HUD `misiones X/6 → RITUAL %`; minimapa marca monitores. FPS estable, consola limpia.

- [ ] **Step 3: README — estado**

En `README.md`, actualiza el bloque de estado:

```markdown
> **Estado:** R5 — misiones: los 8 supervivientes reparan monitores (siempre ocupados); al completarlos todos se desbloquea el ritual (gather → channel → ganar). El rugido acosa (cordura↓ + ralentiza) en vez de dispersar.
```

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: actualiza estado (R5 misiones)"
```

---

## Self-Review (cobertura del spec)

| Requisito del spec | Tarea(s) |
|---|---|
| Fase MISSIONS + `missions[]` + nueva firma createRitual | Task 1 |
| `workMission` + gate MISSIONS→GATHER + cuentas | Task 2 |
| `assignRitualRoles` REPAIR en MISSIONS | Task 3 |
| Estaciones (TV) + render de progreso | Task 4 |
| IA repara (REPAIR candidatos + workMission) | Task 5 |
| Ralentización shaken (tarea + movimiento) | Task 5, Task 6 |
| Rugido → shaken (cordura↓, sin huir/soltar) | Task 6 |
| HUD + minimapa de misiones | Task 7 |
| Verificación node --test + CDP | Tasks 1-3 (node), 4-8 (manual) |
| Tests de R2 migrados a la nueva firma | Task 1 |

**Consistencia de tipos:** `createRitual(missionCells, objectCells, altarCell, opts)`; `PHASE.MISSIONS`; `RROLE.REPAIR`; misiones `{id,gx,gz,progress,done}`; `workMission(ritual,id,dt,rate)`; `missionsDoneCount`/`allMissionsDone`; agente gana `shaken` (s); constantes `NUM_MISSIONS/REPAIR_RATE/SHAKEN_DUR/SHAKEN_SLOW/MISSION_HEIGHT`. La rama GATHER de `updateHunter` conserva el pickup/deposit de R2 intacto.

---

## Execution Handoff

Tras guardar el plan, elige cómo ejecutarlo (ver final del mensaje).
